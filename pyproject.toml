[project]
name = "typemap-test"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.13.4",
    "sqlalchemy>=2.0.49",
    "typemap @ git+https://github.com/iliyasone/python-typemap.git",
]

[dependency-groups]
dev = [
    "mypy @ git+https://github.com/msullivan/mypy-typemap.git",
    "pytest",
]
